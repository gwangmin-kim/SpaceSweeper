## [2026-01-07] 구현 내용

### 🎯 목표
- 공격 시스템 구현
- 자원 수집 구현

### ✅ 진행 상황
- [x] `IDamagable`: 대미지를 받을 수 있는 대상이 갖는 인터페이스. (기본적으론 우주 폐기물, 이후 맵 상의 방해요소인 소행성 등의 개체들도 공격으로 파괴 가능하게 구현할 것을 고려하여 인터페이스 정의) `TakeDamage` 메서드와 `ApplyKnockback` 메서드를 갖는다.
- [x] `SpaceFloatingObject`: 우주 폐기물 클래스 정의. `Rigidbody2D` 기반 부유 움직임. 체력을 가지고, 피격 시 체력이 0 이하가 되면 `Die` 호출. (`Die` 메서드는 현재 비어있음)
- [x] `IWeapon`: 무기가 갖는 인터페이스. `Attack` 메서드를 갖는다.
- [x] `Pickaxe`: 기본 무기인 곡괭이 클래스. 반지름과 각도를 기반으로 부채꼴 범위를 갖는 무기. 범위와 공격 속도, 대미지는 조절 가능(현재는 고정. 추후 `UpgradeManager` 쪽 구현하면서 값을 받아와 초기화할 계획)
- [x] `SpacePlayerController`: 공격 입력에 대한 로직 변경. Button에서 Pass Through 방식으로 변경하여 버튼을 누르고 있으면 지속적으로 공격 함수 (`IWeapon.Attack`)를 호출하게 됨 (현재 0.1초 간격) - comment: 버튼을 누른 시점에 `StartAttack`, 뗀 시점에 `EndAttack`을 호출하는 식으로 변경하고, 반복 공격 로직은 각 무기 클래스 내부에서 구현하는 쪽이 더 나을 것 같음. 추후 수정 고려
- [ ] 폐기물 파괴 시 정해진 만큼 자원 파편이 드롭. `Die` 메서드 구현 필요. 이 수치는 폐기물의 고정값 (혹은 약간의 분산을 둔 범위) 에 플레이어 업그레이드에 따른 배율이 곱해져 결정되도록 함.
- [ ] `SpacePlayerMagnet`: 자원 수집을 위해 플레이어 오브젝트의 자식 오브젝트로 원형 콜라이더를 둠. `OnTriggerEnter2D`에서 자원 파편을 감지하면, 자원 파편의 상태를 변경
- [ ] `ResourceItem`: 자원 파편 클래스. 세 가지 상태를 가짐. 1 생성 (터져나오는 시각적 효과) - 2 부유 (플레이어에게 수집되기 전) - 3 유도 (플레이어 방향으로 끌려감) 1 -> 2는 자동 전환, 이 시간 동안은 끌려가지 않음. 이후 `SpacePlayerMagnet`에서 `_target` 트랜스폼을 설정하면 해당 지점으로 지속적으로 끌려감. 별도의 

### 🚀 트러블 슈팅
**문제 상황:**
특별한 기술적 문제는 발견되지 않음.

### 📝 내일 할 일
- 폐기물 파괴 시 로직 구현
- 자원 파편 수집 매커니즘 구현 완료: 플레이어 주체
- 샷건 및 레이저 건 공격 매커니즘 구현
- 시각적 톤 구체적으로 구상: 도트 vs 캐주얼
- 임시 에셋(스프라이트 등) 구비