## [2025-01-02] 플레이어 이동 및 관성 구현

### 🎯 목표
- 우주 공간의 무중력 느낌을 살린 플레이어 이동(WASD) 구현
- 벽 충돌 처리

### ✅ 진행 상황
- [x] Rigidbody2D 기반 이동 스크립트 작성 (`PlayerController.cs`)
- [x] Input System 연결

### 🚀 트러블 슈팅 (중요!)
**문제 상황:**
우주선이 멈추지 않고 계속 미끄러짐. `drag`를 0으로 했더니 조작이 너무 어려움.

**시도한 방법:**
1. 키를 떼면 속도를 0으로 강제 세팅 -> 너무 딱딱해서 우주 느낌이 안 남.
2. `Rigidbody2D.drag` 값을 1로 올림 -> 너무 뻑뻑함.

**해결:**
가속할 때는 drag를 0으로 하고, 입력이 없을 때만 drag를 0.5로 동적으로 변경하는 로직 적용.
(코드 스니펫 첨부)

### 📝 내일 할 일
- 카메라가 플레이어를 부드럽게 따라가도록 Cinemachine 세팅